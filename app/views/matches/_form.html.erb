<%= form_for(match) do |f| %>
  <% if match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
      <% match.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if @team != nil %>

  <% end %>
  <label>Home Team:</label><%= select(:member, :team_id, @team) %>
  VS
  <label>Away Team:</label><%= select(:member, :team_id, @team) %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
